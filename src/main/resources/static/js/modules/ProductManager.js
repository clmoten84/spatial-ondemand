/*
 * ProductManager.js
 *
 * Defines components and widgets required for the Product tree that allows users
 * to load products/services into the map for defining export.
 */

define(['dojo/_base/declare',
        'dojo/dom',
        'dijit/dijit'], function(declare, dom, dijit){

    // ProductManager class declaration
    declare(null, {
        /************************ Properties ************************** */

        /************************ Class functions *************************/
    });
});